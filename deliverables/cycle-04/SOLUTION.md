# SOLUTION (cycle-04)

This cycle adds a lightweight monitor for TTN group traffic with optional log export.

## Highlights
- New `ttn/monitor.py` module with summary intervals and JSON-lines export.
- `ttn.node` includes a `monitor` subcommand.
- README updated with monitor usage examples.

## Files changed
- `ttn/monitor.py` (new)
- `ttn/node.py` (monitor subcommand)
- `TTN_README.md` (monitor usage)
